## 2026-07-23 Session Log

### 会话概览
连续工作了约 9.5 小时的深度会话，覆盖了 android-webdav-player 项目的大量修复。

### 已完成修复（PR #14）

#### P0 崩溃
- **DB 版本升级 2→3 + MIGRATION_2_3**：`RemoteFileEntity` 添加 `cachedAt` 字段
- **移除 MainActivity 手动 startForegroundService**：Android 12+ 崩溃，Media3 自动管理

#### P1 数据竞争
- **EngineMedia3Adapter**：StateSnapshot 原子快照读取
- **ExoPlayerEngine**：progress 轮询添加 null 检查

#### P2 逻辑修复
- **PlaybackProgressSaver**：Mutex.tryLock() 保护非原子读写
- **SardineWebDavClient**：retryIO 协程化（coroutine delay 替代 Thread.sleep）
- **PlayerViewModel**：Media3 STATE_IDLE + playerError → PlaybackState.ERROR 映射

#### P3 其他
- **ServerConfigStore**：loadAll/saveAll 加同步锁
- **SardineWebDavClient**：指数退避 + MIME 类型恢复
- **RemoteFileDao**：移除重复 clearAllExpired

### AList WebDAV 路径修复（commit 3893570）
- `mapResource` 剥离 basePath（如 `/dav`），解决路径重复问题
- `java.net.URI` 替代被标记 error 的 `HttpUrl.parse`

### 网络明文修复
- `network_security_config.xml` 允许 HTTP（基域 + 127.0.0.1）

### 播放器 UI 重写（commit 35d884a）
- **PlayerScreen 重写**：
  - 视频区使用 PlayerView 自带控制器（`useController=true`），移除下面冗余的 Compose 控制按钮
  - 非全屏：视频用 PlayerView 自带控制；音频用 Compose 控制按钮 + Slider
  - 全屏模式：沉浸式 SystemUI + 强制横屏 + 自定义控制层（Sliders + 快进/快退15秒 + 播放/暂停）
  - 全屏左右滑动手势调整进度
- **VLC 修复**：VlcEngine 添加 SurfaceView + SurfaceHolder.Callback

### 用户反馈与待改进
用户对播放器 UI 不满意：
- 视频播放框里有能用的 PlayerView 控制器了，但下面还有不能用的控制按钮 → 已删除
- 全屏模式 + 滑动调整进度 → 已添加
- libVLC 不能播 → SurfaceView 已加但可能还有问题

### 用户 Profile 更新
- 电脑局域网 IP：192.168.1.105
- AList WebDAV 路径：/dav（baseUrl 拼接后）
- Android 9+ 明文 HTTP 限制已处理
